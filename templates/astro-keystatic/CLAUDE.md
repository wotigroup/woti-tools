<!-- Dit bestand wordt automatisch bij elke Claude Code-sessie geladen (project-root CLAUDE.md). -->
<!-- STACK.md blijft de daadwerkelijke inhoud — dit bestand importeert het alleen, zodat het niet los aangeleverd hoeft te worden. -->

@STACK.md

## Claude Code — aanvullend

- Run `npm run typecheck` en `npm run lint` vóór je een taak als afgerond beschouwt (zie STACK.md §2.3).
- Bij het bouwen/uitbreiden van een sectie: lees eerst de `woti-client-first`-skill (`references/core-structure-and-components.md`) voor de juiste HTML-structuur en insertion point, vóórdat je nieuwe componenten schrijft.
